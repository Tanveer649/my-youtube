import React from "react";

const Button = ({ name }) => {
  return (
    <div>
      {/* <button className="px-3 py-1 m-2 bg-gray-200 text-gray-800 rounded-lg hover:bg-gray-300 active:bg-gray-500 whitespace-nowrap">
        {name}
      </button> */}
    </div>
  );
};

export default Button;
